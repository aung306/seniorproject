module.exports = {
    presets: [
      '@babel/preset-env',    // For modern JavaScript features (ESModules, etc.)
      '@babel/preset-react',  // For JSX support in React
      '@babel/preset-typescript', // For TypeScript support
    ],
    plugins: [
      // Add any Babel plugins if needed (e.g., for class properties or decorators)
    ],
  };
  